set from = "analizer@yandex.ru"
set realname = "Dmitry Potapov"

set imap_user = "analizer"

set folder = "imaps://imap.yandex.ru:993"
set spoolfile = "+INBOX"
set postponed = "+[Yandex]/Drafts"

set header_cache = ~/.mutt/yandex/cache/headers
set message_cachedir = ~/.mutt/yandex/cache/bodies
set certificate_file = ~/.mutt/yandex/certificates
set include = yes
set record = ~/.mutt/yandex/sent

set smtp_url = "smtp://analizer@smtp.yandex.ru:25/"
set smtp_authenticators="login"
unset imap_passive
set imap_check_subscribed
set mail_check = 60
set timeout = 10

set sort = threads
set menu_scroll = yes
set send_charset = "utf-8"
set assumed_charset = "cp1251"
set pager_format = " %C - %[%H:%M] %.20v, %s%* %?H? [%H] ?"
set editor = "vim -c 'set syntax=mail ft=mail enc=utf-8'"

set sidebar_width = 19
set sidebar_visible = yes
set sidebar_delim = '|'

bind index \CP sidebar-prev
bind index \CN sidebar-next
bind index \CO sidebar-open
bind pager \CP sidebar-prev
bind pager \CN sidebar-next
bind pager \CO sidebar-open

macro index b '<enter-command>toggle sidebar_visible<enter>'
macro pager b '<enter-command>toggle sidebar_visible<enter>'

set status_format="./.mutt/awesome-mutt yandex %u|"
auto_view text/html

color sidebar_new blue default
color status brightwhite default
color indicator brightgreen default
